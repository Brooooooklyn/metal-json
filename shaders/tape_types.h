// tape_types.h — on-GPU tape entry layout.
//
// Placeholder: the real tape format (u64 entries, type tag in the high byte,
// containers storing matching-index + count, strings packing
// offset:40/len:24 into the stringbuf) lands in M1 together with
// src/tape.rs and a layout test keeping the two in lock-step.

#ifndef METAL_JSON_TAPE_TYPES_H
#define METAL_JSON_TAPE_TYPES_H

#endif // METAL_JSON_TAPE_TYPES_H
